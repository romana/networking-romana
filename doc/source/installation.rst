============
Installation
============

At the command line::

    $ pip install networking-romana

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-romana
    $ pip install networking-romana
